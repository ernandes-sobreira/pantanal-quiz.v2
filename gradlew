#!/usr/bin/env sh
# Minimal Gradle wrapper script placeholder.
# If Android Studio complains, use "File > Invalidate Caches / Restart" or re-generate wrapper.
DIR="$(cd "$(dirname "$0")" && pwd)"
exec gradle "$@"
